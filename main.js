var ranNum =Math.floor(Math.random()*100)+1;//this the fucntion that enable us to get the random number
for (ranNum=1; ranNum<100; ranNum++)//ranNum is the random number that the computer will select


for(i=1; i<10; i++ )
{
var evenOdd=ranNum%2;//here we want to get the modulus of the random number selected pwith 2
if (evenOdd=0);
console.log("Number is even")
else 
	console.log("number is odd")
}

Let prompt()